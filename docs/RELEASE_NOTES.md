# Release Notes

This file tracks release notes for merges to `main`.

## 2026-03-13

### Frontend

- Task 1: Replaced raw validation JSON with user-friendly login error messaging for invalid email input.
- Task 2: Updated Admin/Global Admin logout behavior to redirect to `/` (home/login entry route).
- Task 3: Added player filter on Session matches page to search and view matches by player.
- Task 4 (frontend integration): Added Playwright local test cleanup hooks to run DB cleanup around E2E runs.

